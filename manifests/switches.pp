
node /^(c3130g01).*/ {
  notify {"${hostname} is a switch":}
}
node /^(c3130g02).*/ {
  notify {"${hostname} is a switch":}
}

node /^(c3560g01).*/ {
  notify {"${hostname} is a switch":}
}
node /^(c3560g02).*/ {
  notify {"${hostname} is a switch":}
}
node /^(c3560g03).*/ {
  notify {"${hostname} is a switch":}
}
node /^(c3560g04).*/ {
  notify {"${hostname} is a switch":}
} 
node /^(c3560g05).*/ {
  notify {"${hostname} is a switch":}
} 

